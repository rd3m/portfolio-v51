import React from "react";
import styles from "./Title.module.scss";

const Title = () => {
    return (
        <div className={styles.title}>
            <span className={styles.title__text}>Riley</span>
        </div>
    );
};

export default Title;
